At project onset, I compiled this with gcc.

Be sure to run the fetch script first to get the sdl libraries we need.

Then navigate to the repo and run these:
1) cmake --preset default
2) cmake --build --preset default
3) ./MyGame
